Known Cmd commands:
	cd
	cd <directory location>
	cd .
	cd ..
	pwd
	ls
	ls -a
	ls -l
	ls -1
	ls -al
	rm <file>
	rm -f <file>
	rm -r <folder>
	rm -rf <file>
	mv <file> <newFileName> (I know how to rename files this way, not sure how mv is to be used outside of renaming though)
	open <file>
	echo <text> > <filename>
	echo <text> >> <filename>

===========
From a prior networking class I also know

	ifconfig

===========
